meta {
  name: delete-user
  type: http
  seq: 3
}

delete {
  url: {{BASE_URL}}/api/v1/profile
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "password": "password123"
  }
}

settings {
  encodeUrl: true
}
