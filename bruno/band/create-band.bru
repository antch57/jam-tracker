meta {
  name: create-band
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/api/v1/bands
  body: json
  auth: inherit
}

body:json {
  {
    "band_name": "goose",
    "genre": "jam",
    "description": "slaps"
  }
}

settings {
  encodeUrl: true
}
